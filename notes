un programme qui lit par block de n.

split OF=filein (stdin default) OF=file1 OF=file2 OF=file3 key=FF0BAD bs=1024
merge IF=file1 IF=file2 IF=file3 key=FF0BAD bs=1024 OF=fileout (stdout default)






for (unsigned int i = 0; i < 41; ++i) { values[i] = malloc(str_length * sizeof(**values)); }

13:52 <thlc> char **values = malloc(41 * sizeof(*values));

--

todo :

test 00 in key to reject value

add one item in the default key, if default key is in use and number of volumes match

display limit in help (1024 volumes etc)

